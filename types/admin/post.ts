export enum PostStatus {
  PUBLISHED = "published",
    DRAFT = "draft",
    DELETED = "deleted",
    WATING_APPROVE = "wating_approve",
    HIDDEN = "hidden",
    APPROVE_CANCELED = "approve_canceled",
}
export enum UserStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  DELETED = "deleted",
  BANNED = "banned",
}

export enum UserRole {
  ADMIN = "admin",
  USER = "user",
  STUDENT = "student",
  MANAGER = "manager",
}
